# Quingo Auth Service

Микросервис аутентификации и авторизации для платформы Quingo.

## Основные возможности и фичи

*   **API-First подход**: Проектирование контрактов api с использованием **TypeSpec**.
*   **Двухуровневая модель токенов**:
    *   **Access Token**: Короткоживущие токены для авторизации запросов.
    *   **Refresh Token**: Долгоживущие токены с механизмом ротации.
*   **White-list**:
    *   Отслеживание активных access токенов в Redis с возможностью мгновенного отзыва.
*   **Outbox Pattern**:
    *   Внешние события сначала регистрируются в БД, что позволяет не потерять их даже при падении сервиса.
    *   Фоновый **Outbox Scheduler** гарантирует доставку сообщения в Kafka даже при временных сбоях сети.
*   **Интеграция с Kafka**: Асинхронная публикация событий в топик `notifications-events` для последующей обработки микросервисом уведомлений.
*   **OAuth2 авторизация**: Поддержана инициализация входа через Google и GitHub с возможностью слияния с локальным аккаунтом.

## 🛠 Технологический стек

| Технология             | Роль в проекте                                                               |
|:-----------------------|:-----------------------------------------------------------------------------|
| **Java 21**            | Основной язык                                                                |
| **Spring Boot 3.5.9**  | Базовый фреймворк приложения                                                 |
| **Spring Security 6**  | Обеспечение безопасности, разделение на роли                                 |
| **PostgreSQL 17**      | Основная БД микросервиса                                                     |
| **Redis**              | White-list access-токенов, хранение токенов верификации почты с TTL 60 минут |
| **Kafka 4.0.2**        | Асинхронная система обмена сообщениями с микросервисом уведомлений           |
| **Kubernetes (k3s)**   | Оркестрация контейнеров и управление инфраструктурой кластера                |
| **Flyway**             | Миграции и версионирование схемы базы данных                                 |
| **TypeSpec**           | Описание API и генерация OpenAPI спецификации                                |
| **MapStruct / Lombok** | Вспомогательные инструменты для маппинга и уменьшения boilerplate-кода       |

## 🏗 Инфраструктура и Деплой

Сервис полностью контейнеризирован и адаптирован для работы в облачных средах:
*   **Kubernetes (k3s)**: Оркестрация контейнеров в легковесном кластере.
*   **Ingress Nginx**: Управление внешним трафиком и маршрутизация.
*   **Kafdrop**: Интегрированный Web-интерфейс для мониторинга топиков и сообщений в Kafka.
*   **Swagger UI**: Сервис интерактивной документации. Собирает документацию api с каждого микросервиса платформы.

### CI/CD и автоматизация
В проекте настроены пайплайны:
*   **Автоматическое обновление документации**: При изменении TypeSpec-моделей новая OpenAPI спецификация генерируется и деплоится автоматически, обеспечивая актуальность.
*   **Основной пайплайн**: 
     * **Тестирование:** Прогоняются unit и интеграционные тесты по кодовой базе.
    * **Сборка имаджа:** При успешном прохождении тестов собирается образ приложения и пушится на докерхаб.
    * **Деплой:** После успешной сборки имадж деплоймента в k3s кластере обновляется на новую версию.


### Тестирование
Для обеспечения стабильности используется многоуровневое тестирование:
*   **Unit-тесты**: Покрытие бизнес-логики сервисов с использованием JUnit 5 и Mockito.
*   **Integration-тесты**: Тестирование взаимодействия с реальными компонентами (PostgreSQL, Redis, Kafka) с помощью **Testcontainers**, что гарантирует корректную работу в среде, идентичной продакшену.

## 📋 Документация и Мониторинг

Базовый путь API: `https://quingo.arhr.tech/api/auth`.

После деплоя в кластер инструменты доступны по следующим адресам:
*   **API Documentation**: [https://docs-quingo.arhr.tech/](https://docs-quingo.arhr.tech/) — здесь можно протестировать все эндпоинты сервиса.
*   **Kafka UI (Kafdrop)**: [https://kafdrop-quingo.arhr.tech/](https://kafdrop-quingo.arhr.tech/) — просмотр сообщений в топиках Kafka.
*   **API сервиса**: https://quingo.arhr.tech/api/auth — api микросервиса авторизации.


## 💻 Quick Start

1.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/quingo-quiz/auth-service.git
    cd auth-service
    ```
2.  **Создайте файл `.env`** в корне проекта на основе примера ниже.

#### Пример `.env` файла
```properties
# App Config
APP_DOMAIN=localhost
SPRING_PROFILES_ACTIVE=local

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=auth-service-db

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=admin
REDIS_MAXMEMORY=256mb

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# JWT
JWT_ISSUER=quingo.arhr.tech
JWT_SECRET=your_very_long_and_secure_secret_string_here
JWT_ACCESS_MINUTES=15
JWT_REFRESH_DAYS=30
```
3.  **Запустите приложение**:
    ```bash
    docker-compose up -d --build
    ```

4. **Теперь приложение доступно по http://localhost:8080/api/auth**

## 🔮 Планы по развитию

*   [ ] **Переход на RS256**: Использование асимметричного шифрования для подписи JWT.
*   [ ] **Продвинутый мониторинг**: Мониторинг со связкой Prometheus + Grafana.
*   [ ] **Test Coverage**: Увеличение покрытия Unit и Integration тестами.
