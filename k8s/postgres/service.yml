apiVersion: v1
kind: Service
metadata:
  name: auth-postgres-service
  namespace: quingo-app
spec:
  selector:
    app: auth-postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None